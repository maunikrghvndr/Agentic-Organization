# Local tool state
.claude/

# Pre-git backup (baseline is preserved as the first commit; folder kept on disk only)
_archive/

# Cloud-sync temp artifacts
.tmp.driveupload/

# Graphify derived graph (any level)
graphify-out/
